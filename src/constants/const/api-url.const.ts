export const baseUrl = process.env.NEXT_PUBLIC_BASE_URL;

export const loginUrl = `${baseUrl}api/v1/cms/login`;

export const collectionUrl = `${baseUrl}api/v1/cms/collections?offset=0&limit=10`;

export const photoUrl = `https://jsonplaceholder.typicode.com/photos`;
